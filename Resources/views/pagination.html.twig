 {% if pagination.pages %}
    <script>
	    $(function() {
	        $( ".pagination" ).buttonset();
		    $( "a, button", ".pagination" ).button();
		    $( "button", ".pagination" ).button( "disable" )
	    });
	</script>
 <div class="pagination" >
    <ul>
        <li>
            <a title="First Page" href="{{ path(app.request.attributes.get('_route')) }}?{{ pagination.param }}=1{{ pagination.query }}">First</a>
        </li>
        {% for p in pagination.pages %}
        <li>
        {% if p == pagination.current %}
            <button>{{ p }}</button>
            {% else %}
            <a title="Page {{ p }}" href="{{ path(app.request.attributes.get('_route')) }}?{{ pagination.param }}={{ p }}{{ pagination.query }}">{{ p }}</a>
            {% endif %}
        </li>
        {% endfor %}
        <li>
            <a title="Last Page" href="{{ path(app.request.attributes.get('_route')) }}?{{ pagination.param }}={{ pagination.last }}{{ pagination.query }}">Last</a>
        </li>
    </ul>
 </div>
 {% endif %}
