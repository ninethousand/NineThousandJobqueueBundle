<h1>Arg creation</h1>

<form action="{{ path('jobqueue_arg_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('jobqueue_arg') }}">
            Back to the list
        </a>
    </li>
</ul>
