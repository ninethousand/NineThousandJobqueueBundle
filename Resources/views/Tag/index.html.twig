<h1>Tag list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Job</th>
            <th>Value</th>
            <th>Active</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('jobqueue_tag_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.job }}</td>
            <td>{{ entity.value }}</td>
            <td>{{ entity.active }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('jobqueue_tag_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('jobqueue_tag_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('jobqueue_tag_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
