<h1>Job creation</h1>

<form action="{{ path('jobqueue_job_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('jobqueue_job_') }}">
            Back to the list
        </a>
    </li>
</ul>
